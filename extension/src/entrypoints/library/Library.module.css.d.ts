/**
 * Ambient declaration for Library.module.css — same pattern as the popup's.
 * Explicit keys so typos in Library.tsx surface in TypeScript.
 */
declare const styles: {
  readonly page: string;
  readonly header: string;
  readonly title: string;
  readonly subtitle: string;
  readonly search: string;
  readonly chips: string;
  readonly chip: string;
  readonly chipActive: string;
  readonly grid: string;
  readonly card: string;
  readonly cardHighlight: string;
  readonly cardImg: string;
  readonly cardTile: string;
  readonly cardBody: string;
  readonly cardClass: string;
  readonly cardTitle: string;
  readonly cardDesc: string;
  readonly cardQuote: string;
  readonly cardTags: string;
  readonly tag: string;
  readonly cardFooter: string;
  readonly cardDate: string;
  readonly cardActions: string;
  readonly actionBtn: string;
  readonly actionDanger: string;
  readonly empty: string;
  readonly emptyOrb: string;
};
export default styles;
